name: Report Generation Workflow

on:
  push:
    branches:
      - main

jobs:
  run-ui-tests:
    runs-on: ubuntu-latest
    outputs:
      test_results: ${{ steps.test.outputs.results }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Make UI test script executable
        run: chmod +x ./uitest.sh
      
      - name: Run UI Tests
        id: test
        run: |
          # Run tests and capture output
          TEST_OUTPUT=$(./uitest.sh)
          echo "results=$TEST_OUTPUT" >> $GITHUB_OUTPUT

  run-unit-tests:
    runs-on: ubuntu-latest
    outputs:
      test_results: ${{ steps.test.outputs.results }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Make unit test script executable
        run: chmod +x ./unittest.sh
      
      - name: Run Unit Tests
        id: test
        run: |
          # Run tests and capture output
          TEST_OUTPUT=$(./unittest.sh)
          echo "results=$TEST_OUTPUT" >> $GITHUB_OUTPUT

  generate-reports:
    needs: [run-ui-tests, run-unit-tests]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Create UI Test Report
        run: |
          echo "UI Test Results:" > ui_test_report.md
          echo "${{ needs.run-ui-tests.outputs.test_results }}" >> ui_test_report.md

      - name: Create Unit Test Report
        run: |
          echo "Unit Test Results:" > unit_test_report.md
          echo "${{ needs.run-unit-tests.outputs.test_results }}" >> unit_test_report.md

      - name: Upload UI Test Report
        uses: actions/upload-artifact@v3
        with:
          name: ui-test-report
          path: ui_test_report.md

      - name: Upload Unit Test Report
        uses: actions/upload-artifact@v3
        with:
          name: unit-test-report
          path: unit_test_report.md
